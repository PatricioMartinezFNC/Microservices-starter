spring:
  application:
    name: ms-common-secret, ms-common
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://ic.pdr.local}
server:
   port: 9081
   compression:
      enabled: true
   servlet.session:
      # Session timeout after 1 minute
      timeout: 120